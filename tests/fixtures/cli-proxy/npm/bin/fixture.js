#!/usr/bin/env node
// Minimal CLI proxy fixture entrypoint. A real proxy would resolve and exec
// the downloaded native binary; the fixture just prints and exits cleanly.
console.log("cli-proxy-fixture ok");
process.exit(0);
