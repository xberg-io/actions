name: Setup Node.js Workspace
description: Configure Node.js with pnpm and workspace dependencies
inputs:
  node-version:
    description: Node.js version to install
    required: false
    default: "24"
  frozen-lockfile:
    description: Use frozen lockfile for pnpm install
    required: false
    default: "false"
  ignore-scripts:
    description: >
      Skip running install scripts (e.g. native-binding postinstalls).
      Use when the workspace only needs JS tooling (lint, format) and not
      compiled native modules — avoids node-gyp builds that may fail on
      newer Node ABIs.
    required: false
    default: "false"
  registry-url:
    description: npm registry URL for authentication (e.g., https://registry.npmjs.org/)
    required: false
    default: ""
runs:
  using: composite
  steps:
    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: ${{ inputs.node-version }}
        registry-url: ${{ inputs.registry-url != '' && inputs.registry-url || null }}

    - name: Install pnpm via npm
      shell: bash
      # Install pnpm as a JS package via the system Node.js — avoids
      # `@pnpm/exe`, whose SEA-built standalone binary is broken on
      # Intel macOS (darwin-x64) and requires npm-on-PATH on ARM Linux
      # (not present before actions/setup-node runs).
      # Upstream: https://github.com/pnpm/pnpm/issues/11423
      # + https://github.com/nodejs/node/issues/62893.
      # Lockfile 9.0 is forward/backward compatible between pnpm 10
      # and 11, so CI on 10 stays compatible with local dev on 11.5.1.
      run: npm install --global pnpm@10

    - name: Install dependencies
      shell: bash
      run: |
        args=()
        if [ "${{ inputs.frozen-lockfile }}" = "true" ]; then
          args+=(--frozen-lockfile)
        else
          # pnpm defaults frozen-lockfile=true in CI; pass the explicit negation
          # so `frozen-lockfile: false` is honored (e.g. napi workspaces whose
          # platform optionalDependencies pin a not-yet-published version that
          # cannot be recorded in the lockfile).
          args+=(--no-frozen-lockfile)
        fi
        if [ "${{ inputs.ignore-scripts }}" = "true" ]; then
          args+=(--ignore-scripts)
        fi
        pnpm install "${args[@]}"

    - name: Add pnpm bin to PATH
      shell: bash
      run: echo "$(pnpm bin)" >> "$GITHUB_PATH"
