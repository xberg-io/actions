name: Test setup-maven
on:
  push:
    branches: [main]
    paths:
      - "setup-maven/**"
      - ".github/workflows/test-setup-maven.yml"
  pull_request:
    paths:
      - "setup-maven/**"
      - ".github/workflows/test-setup-maven.yml"

permissions:
  contents: read

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7

      - name: Setup Java 21 (required by Maven 3.9+)
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "25.0.4+101.0.LTS"

      - name: Setup Maven
        uses: ./setup-maven

      - name: Verify Maven installation
        shell: bash
        run: mvn --version

      - name: Verify Java installation
        shell: bash
        run: java -version
