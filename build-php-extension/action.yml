name: Build PHP Extension
description: Builds PHP extension using ext-php-rs (Linux and Windows)

inputs:
  crate-name:
    description: Cargo crate name for the PHP extension
    required: true
  lib-name:
    description: Name of the built library (without lib prefix or extension)
    required: true
  php-version:
    description: PHP version (only used on Windows for thread safety)
    required: false
    default: ""
  php-ts:
    description: Thread safety mode - ts or nts (Windows only)
    required: false
    default: ""
  rewrite-native-deps:
    description: >
      Rewrite workspace path-deps to registry version-deps via alef before
      building, so a source-distributed extension resolves on a consumer machine.
      Disable for non-release CI that builds against the local workspace.
    required: false
    default: "true"
  alef-version:
    description: "Alef version for the dependency rewrite (passed to install-alef@v1)"
    required: false
    default: "latest"
  dry-run:
    description: >
      Skip the rewrite-native-deps step when true. Dry-run publish workflows run
      before the core crate is on crates.io, so requiring the registry would
      hard-fail on a chicken-and-egg version lookup.
    required: false
    default: "false"
  cargo-features:
    description: >
      Comma-separated Cargo features to enable for the extension build (passed to
      `cargo build --features`). Required when the crate gates its `get_module`
      entrypoint behind a feature (e.g. `extension-module`); an unfeatured build
      then produces an empty cdylib with no PHP module symbol.
    required: false
    default: ""

outputs:
  extension-path:
    description: Path to the built extension file (.so on Linux, .dll on Windows)
    value: ${{ steps.build-workspace.outputs.extension-path || steps.build-unix.outputs.extension-path || steps.build-windows.outputs.dll }}

runs:
  using: composite
  steps:
    - name: Rewrite native dependencies to registry versions
      if: inputs.rewrite-native-deps == 'true' && inputs.dry-run != 'true'
      uses: xberg-io/actions/rewrite-native-deps@v1
      with:
        lang: php
        alef-version: ${{ inputs.alef-version }}
        require-registry: "true"

    - name: Build PHP extension (Linux/macOS)
      if: runner.os != 'Windows' && inputs.rewrite-native-deps == 'true' && inputs.dry-run != 'true'
      id: build-unix
      shell: bash
      env:
        CRATE_NAME: ${{ inputs.crate-name }}
        LIB_NAME: ${{ inputs.lib-name }}
        CARGO_FEATURES: ${{ inputs.cargo-features }}
      run: |
        set -euo pipefail
        # ~keep The build script prints the extension path as its last stdout line.
        # Keep only that line and write it with the heredoc delimiter form: a single
        # stray progress line ahead of it used to make this a multi-line value whose
        # extra line carried no `=`, and the runner then rejected the entire file
        # command ("Unable to process file command 'output' successfully").
        extension_path=$( \
          bash "${{ github.action_path }}/scripts/build-out-of-workspace.sh" \
          "$CRATE_NAME" "$LIB_NAME" "$GITHUB_WORKSPACE" | tail -n 1
        )
        {
          echo "extension-path<<GHA_EXTENSION_PATH_EOF"
          echo "$extension_path"
          echo "GHA_EXTENSION_PATH_EOF"
        } >> "$GITHUB_OUTPUT"

    - name: Install LLVM for libclang (Windows)
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        $ErrorActionPreference = "Stop"
        choco install llvm --version=18.1.8 --allow-downgrade -y
        # ~keep `$ErrorActionPreference = "Stop"` does not abort on a native command's
        # non-zero exit, so an install failure would otherwise be reported below as a
        # missing libclang.dll rather than as the install failure it is.
        if ($LASTEXITCODE -ne 0) {
          throw "choco install llvm failed with exit code $LASTEXITCODE"
        }
        $llvmPath = "C:\Program Files\LLVM\bin"
        if (-not (Test-Path (Join-Path $llvmPath "libclang.dll"))) {
          throw "libclang.dll not found in LLVM installation"
        }
        "LIBCLANG_PATH=$llvmPath" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
        "PATH=$llvmPath;$env:PATH" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

    - name: Build PHP extension from workspace
      if: inputs.rewrite-native-deps != 'true' || inputs.dry-run == 'true'
      id: build-workspace
      shell: bash
      env:
        CRATE_NAME: ${{ inputs.crate-name }}
        LIB_NAME: ${{ inputs.lib-name }}
        CARGO_FEATURES: ${{ inputs.cargo-features }}
      run: |
        set -euo pipefail
        extension_path=$(bash "${{ github.action_path }}/scripts/build-in-workspace.sh" \
          "$CRATE_NAME" "$LIB_NAME" "$GITHUB_WORKSPACE")
        {
          echo "extension-path<<GHA_EXTENSION_PATH_EOF"
          echo "$extension_path"
          echo "GHA_EXTENSION_PATH_EOF"
        } >> "$GITHUB_OUTPUT"

    - name: Build PHP extension (Windows)
      if: runner.os == 'Windows' && inputs.rewrite-native-deps == 'true' && inputs.dry-run != 'true'
      id: build-windows
      shell: pwsh
      env:
        RUSTC_BOOTSTRAP: "1"
        CRATE_NAME: ${{ inputs.crate-name }}
        LIB_NAME: ${{ inputs.lib-name }}
        CARGO_FEATURES: ${{ inputs.cargo-features }}
        CFLAGS_x86_64_pc_windows_msvc: "/MD"
        CXXFLAGS_x86_64_pc_windows_msvc: "/MD"
        CFLAGS_i686_pc_windows_msvc: "/MD"
        CXXFLAGS_i686_pc_windows_msvc: "/MD"
      run: |
        $ErrorActionPreference = "Stop"

        # ~keep `Stop` does not apply to native commands: a failing `cargo` leaves the
        # script running. Without this check a manifest-parse error surfaced pages later
        # as `Copy-Item` reporting a missing .dll, which reads as a packaging bug rather
        # than the build failure it is.
        function Assert-NativeSuccess {
          param([Parameter(Mandatory = $true)][string]$Operation)
          if ($LASTEXITCODE -ne 0) {
            throw "$Operation failed with exit code $LASTEXITCODE"
          }
        }

        $workspaceRoot = $env:GITHUB_WORKSPACE
        $crateDir = Join-Path $workspaceRoot "crates" $env:CRATE_NAME

        if (-not (Test-Path $crateDir)) {
          $crateDir = Join-Path $workspaceRoot "packages" $env:CRATE_NAME
        }

        if (-not (Test-Path $crateDir)) {
          throw "Crate directory not found"
        }

        $buildTemp = New-Item -ItemType Directory -Path (Join-Path $env:RUNNER_TEMP "build-$([Guid]::NewGuid())")
        Copy-Item -Path $crateDir -Destination (Join-Path $buildTemp "crate") -Recurse
        Set-Location (Join-Path $buildTemp "crate")

        # ~keep The crate now sits outside the workspace, so every inherited key has to be
        # replaced with the workspace's concrete value. Deleting the lines is not enough:
        # `version`, `edition` and `license` are required for the manifest to parse.
        # ~keep The copy holds the binding crate alone, so a surviving `path = "../<core>"`
        # resolves against the build dir and cargo dies on "failed to read
        # <build-dir>\<core>\Cargo.toml". The Linux/macOS branch has stripped those since
        # 579b492; this branch never did, which is why only `PHP (windows)` failed.
        $cargoToml = Join-Path (Get-Location) "Cargo.toml"
        if (Test-Path $cargoToml) {
          & "${{ github.action_path }}/scripts/deinherit-workspace.ps1" $cargoToml (Join-Path $workspaceRoot "Cargo.toml")
          & "${{ github.action_path }}/scripts/strip-internal-paths.ps1" $cargoToml
        }

        # ~keep Seed from the workspace lock so this build tracks the same dependency
        # versions as every other job. This branch previously ran a bare
        # `cargo generate-lockfile` and never copied the workspace lock at all, so it
        # re-resolved to latest on every run — the same defect that let a broken
        # `zune-core` release into the PHP job while `--locked` jobs stayed green.
        # `--locked` is not usable here: resolving `workspace = true` above changes the
        # manifest, so the seeded lock legitimately needs a minimal update.
        $workspaceLock = Join-Path $workspaceRoot "Cargo.lock"
        if (Test-Path $workspaceLock) {
          Copy-Item -Path $workspaceLock -Destination (Join-Path (Get-Location) "Cargo.lock")
        } else {
          cargo generate-lockfile
          Assert-NativeSuccess "cargo generate-lockfile"
        }
        # Pin `time` to 0.3.47 to avoid the trait-impl conflict between `time` 0.3.48
        # and `cookie` 0.18.1 (E0119 in cookie 0.18.1). Tolerate absence in the graph.
        cargo update -p time --precise 0.3.47
        if ($LASTEXITCODE -ne 0) {
          Write-Host "time crate not in dep graph; skipping pin"
          $global:LASTEXITCODE = 0
        }
        if ($env:CARGO_FEATURES) {
          cargo build --release --features $env:CARGO_FEATURES
        } else {
          cargo build --release
        }
        Assert-NativeSuccess "cargo build --release"

        $builtDll = Join-Path (Get-Location) "target\release\$($env:LIB_NAME).dll"
        if (-not (Test-Path $builtDll)) {
          throw "cargo build --release succeeded but did not produce $builtDll; check the lib-name input and the crate's [lib] crate-type"
        }

        $dllPath = Join-Path $workspaceRoot "target\release\$($env:LIB_NAME).dll"
        New-Item -ItemType Directory -Force -Path (Split-Path $dllPath) > $null
        Copy-Item -Path $builtDll -Destination $dllPath

        Set-Location $workspaceRoot
        Remove-Item -Path $buildTemp -Recurse -Force

        "dll=$dllPath" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
